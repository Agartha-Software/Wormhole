# ATP Features

| ID | Fonctionnalité | Catégorie | Objectif | Prérequis | Étapes | Résultats Attendus | Statut | Conformité EIP | Feedback |
|---|---|---|---|---|---|---|---|---|---|
| S0 | Access Documentation | **<span style="color: blue;">User interface</span>** | Documentation is complete and easy to understand | None | 1. Read the project readme 2. Read the relevant documents linked in the readme | The user has the necessary informations to start using Wormhole | **<span style="color: green;">Done</span>** | | |
| S1 | Linux distribution support | **<span style="color: green;">Compatibility</span>** | Install on Debian/Ubuntu/Arch/NixOS/Fedora | Linux machine | 1. Install using your distribution's specific package (guide) | Wormholed and CLI installed | **<span style="color: green;">Done</span>** | | |
| S2 | Windows Installer | **<span style="color: green;">Compatibility</span>** | Install on Windows using the release installer | Windows 10+ with WinFSP | 1. Download the installer 2. Run the installer | Wormhole and its dependencies installed | **<span style="color: green;">Done</span>** | | |
| S2 | Background service via systemd | **<span style="color: green;">Compatibility</span>** | Run in background on Linux | Linux with systemd | 1. Install wormholed and enable service with systemctl | Service enabled and running | **<span style="color: green;">Done</span>** | | Auto "systemd service enable only" works on SOME distro specific packages (.deb, Arch and NixOS). For others, just start 'wormhold' manually. Check docs |
| S3 | Windows support (WinFSP) | **<span style="color: green;">Compatibility</span>** | Run on Windows 10+ | Windows 10+ with WinFSP | 1. run wormholed.exe | Service running | **<span style="color: green;">Done</span>** | | |
| S4 | Install via Cargo | **<span style="color: green;">Compatibility</span>** | Install from crates.io | Linux machine and cargo installed | 1. Install the fuse3 dependancy 2. cargo install wormhole-fs | Binaries built and installed | **<span style="color: green;">Done</span>** | | Should work if you already have Fuse installed on your system. Will crash if it's not. |
| S5 | Create a network using Docker | **<span style="color: orange;">Secondary function</span>** | Easily spin up a network with docker compose | Docker 2.30+ installed | 1. Use docker compose up 2. Enter each of the three created containers with bash 3. Create, read and edit files as usual | 3 containers connected on the same Wormhole Network | **<span style="color: green;">Done</span>** | | |
| S6 | Start the service | **<span style="color: red;">Critical feature</span>** | Start wormholed | Wormhole installed | 1. Run wormholed with default or given socket (-s option) | Service start and listens on desired socket | **<span style="color: green;">Done</span>** | | |
| S7 | Stop the service | **<span style="color: red;">Critical feature</span>** | Stop wormholed | Service running | 1. Stop via ctrl-d or sigterm stops all pods cleanly | Service stops cleanly | **<span style="color: green;">Done</span>** | | |
| CLI0 | List help | **<span style="color: red;">Critical feature</span>** | Show commands help | Wormhole installed | 1. run wormhole --help 2. run wormhole --help COMMAND for each command | usage guides are clear and accurate | **<span style="color: green;">Done</span>** | | |
| CLI1 | Create a Wh network | **<span style="color: red;">Critical feature</span>** | Create a connexion between 2 computers | Wormhole installed on 2 computers | 1. Create pod on one computer with the cli 2. Create a pod on a second computer and connect it to the first one | Connection established | **<span style="color: green;">Done</span>** | | |
| FS1 | Create files and folders | **<span style="color: red;">Critical feature</span>** | Create files at root and inside folders | A network with 2 or more pods connected | 1. Create a file at root 2. Write then append text 3. Create a folder at root 4. Create a subfolder 5. Create a file inside the subfolder 6. Read the file from another pod 7. Create a relative symbolic link 8. Create an absolute symbolic link | Files and folders are visible and readable on each computer | **<span style="color: green;">Done</span>** | | |
| FS2 | Move files and folder | **<span style="color: red;">Critical feature</span>** | Move items within WH and between WH and classic FS | A network with 2 or more computer connected | 1. Move a file within WH 2. Move an empty folder within WH 3. Move a non-empty folder within WH 4. Move a file from classic FS <-> WH 5. Move a non-empty folder classic FS <-> WH | Items can be moved inside and outside the Wormhole mountpoint seamlessly | **<span style="color: green;">Done</span>** | | |
| FS3 | Rename and delete | **<span style="color: red;">Critical feature</span>** | Rename entries and delete folders (including recursive) | A network with 2 or more computer connected | 1. Rename a file 2. Rename a folder 3. Delete a file 4. Delete an empty folder 5. Delete a non-empty folder recursively | Rename works as expected, Folders and files can be deleted | **<span style="color: green;">Done</span>** | | |
| FS4 | List directory content | **<span style="color: red;">Critical feature</span>** | List entries of a directory | A network with 2 or more computer connected | 1. List directory in WH mount | All files/folders are listed as on a normal system | **<span style="color: green;">Done</span>** | | |
| FS5 | Show properties of file/folder | **<span style="color: red;">Critical feature</span>** | Inspect metadata | A network with 2 or more computer connected | 1. Open properties or run stat on entry | Size, timestamps and permissions can be checked | **<span style="color: green;">Done</span>** | | |
| FS6 | Change file and folder permissions | **<span style="color: red;">Critical feature</span>** | Permission enforcement (on linux) | A network with 2 or more computer connected | 1. Change permission's file with chmod 2. Change permission's folder with chmod | Operations follow permissions rules on all linux peers | **<span style="color: green;">Done</span>** | | |
| FS7 | Change file and folder permissions | **<span style="color: red;">Critical feature</span>** | Permission enforcement (on windows) | A network with 2 or more computer connected | 1. Change permission's file with the context menu 2. Change permission's folder with the context menu | Operations follow permissions rules on all windows peers | **<span style="color: orange;">In progress</span>** | | |
| FS8 | Extended attributes (xattr) | **<span style="color: orange;">Secondary function</span>** | Manage xattrs | A network with 2 or more computer connected | 1. setfattr/getfattr on a file | Xattrs propagated/visible as expected | **<span style="color: green;">Done</span>** | | |
| CLI2 | List network tree | **<span style="color: blue;">User interface</span>** | Visualize Wormhole files and their hosts as a tree | Network running | 1. Run tree command | Tree with host mapping displayed | **<span style="color: green;">Done</span>** | | |
| CLI3 | Get hosts for file | **<span style="color: blue;">User interface</span>** | See file hosts | Network running | 1. Run get-hosts with path | Hosts list displayed for this file | **<span style="color: green;">Done</span>** | | |
| CLI4 | Configuration Applied | **<span style="color: orange;">Secondary function</span>** | Configuration files applied without colliding with flags | Configuration files present | 1. Create a new Pod with the new command 2. Use commands like 'inspect' to check the pod state | Local or Global config is applied | **<span style="color: green;">Done</span>** | | |
| CLI5 | Generate configuration | **<span style="color: blue;">User interface</span>** | Write current configuration to disk | Network running and without configuration files | 1. Run "config generate" on a specific target | Configuration files should be generated with the values specified at target creation | **<span style="color: green;">Done</span>** | | |
| CLI6 | Overwrite configuration | **<span style="color: blue;">User interface</span>** | Overwrite current configuration to disk | Network running and without configuration files | 1. Run "config generate" on a specific target 2. Edit the generated configuration 3. Run "config generate" with the "-f" flag | The non applied changes should be reverted in the configuration file | **<span style="color: green;">Done</span>** | | |
| CLI7 | Check configuration | **<span style="color: blue;">User interface</span>** | Check if configuration is valid | Network running and with configuration files present | 1. Modify one of the config files with valid and invalid values 2. Run the "config check" command | The command output should state if the configuration is valid or log the error | **<span style="color: green;">Done</span>** | | |
| CLI8 | Show configuration | **<span style="color: blue;">User interface</span>** | Show current configuration | Network running and without configuration files | 1. Run the "config show" command | Current pod configuration should be displayed in the terminal | **<span style="color: green;">Done</span>** | | |
| CLI9 | Inspect a pod | **<span style="color: blue;">User interface</span>** | Display configuration and connections of a pod | Network running | 1. Run inspect command on target pod | Can check pod current information | **<span style="color: green;">Done</span>** | | |
| CLI10 | Remove a pod | **<span style="color: red;">Critical feature</span>** | Remove a pod from the network | A network with 2 or more computer connected | Run the remove command | The named pod will be deleted, other pods will continue to function | **<span style="color: green;">Done</span>** | | |
| CLI11 | Pod Restart | **<span style="color: blue;">User interface</span>** | Restart a pod | A network with 2 or more computer connected | 1. Create a network with at least 2 machines 2. Use the restart command 3. Inspect the service | The pod restarted correctly and is connected to the same network | **<span style="color: green;">Done</span>** | | |
| CLI12 | Status | **<span style="color: blue;">User interface</span>** | Check node status | Service reachable | 1. Run the status command on a started service | The command succeed if the service is launched on the asked socket | **<span style="color: green;">Done</span>** | | |
| S6 | Redundant copies | **<span style="color: orange;">Secondary function</span>** | Keep multiple replicas | Network running | 1. Configure redundancy to 1 or more 2. Create a network with 3 pods 3. Create a file on one pod 4. Remove the pod detaining the file 5. cat the file content on one of the two other pods | No file went missing | **<span style="color: green;">Done</span>** | | |
| S7 | Use the REST api to control Wormhole | **<span style="color: orange;">Secondary function</span>** | Being able to control Wormhole through a web api | Network running | 1. Install and launch wormholed 2. Check that it's running with the cli "status" command 3. Send commands by posts requests to the tcp port | Behavior must match expected behavior from the CLI | **<span style="color: green;">Done</span>** | | |
| S8 | Automatically reconnect | **<span style="color: orange;">Secondary function</span>** | Be able to automatically reconnect to a network when re-launching the service | A network with 2 or more computer connected | 1. Create a network with at least 2 machines 2. Stop the service on one machine 3. Restart the service on that machine 4. Inspect the service | The service contains the pod that was there before stopping and is connected to the same network | **<span style="color: green;">Done</span>** | | |

